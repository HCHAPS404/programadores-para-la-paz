Ruta POST /reporte

Qué hace:
Recibe un JSON con el campo mensaje y responde confirmando que llegó.

Para qué sirve en una app comunitaria:
Un vecino podría mandar un aviso o reporte desde una página y el servidor lo recibiría de forma ordenada.
